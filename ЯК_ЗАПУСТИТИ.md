# Як запустити проект "Країна Мрій"

Детальна інструкція з запуску Backend, Frontend та Mobile додатку.

---

## 📋 Передумови

### Обов'язкові інструменти:

1. **Node.js** версія >= 18
   - Завантажити: https://nodejs.org/
   - Перевірити версію: `node --version`

2. **npm** (встановлюється разом з Node.js)
   - Перевірити версію: `npm --version`

3. **MongoDB**
   - Локально: https://www.mongodb.com/try/download/community
   - Або використовуйте MongoDB Atlas (хмарна БД): https://www.mongodb.com/cloud/atlas

### Для Mobile додатку (додатково):

4. **React Native CLI**
   ```bash
   npm install -g react-native-cli
   ```

5. **Для iOS:**
   - Xcode (тільки для macOS)
   - CocoaPods: `sudo gem install cocoapods`

6. **Для Android:**
   - Android Studio
   - Android SDK
   - Емулятор або підключений пристрій

---

## 🚀 Швидкий старт

### Порядок запуску:

1. **Backend** (обов'язково першим)
2. **Frontend** (залежить від Backend)
3. **Mobile** (залежить від Backend)

---

## 1️⃣ Backend (NestJS + MongoDB)

### Крок 1: Встановлення залежностей

```bash
cd backend
npm install
```

**Якщо виникає помилка конфлікту залежностей (ERESOLVE):**

```bash
# Варіант 1: Використати legacy-peer-deps (рекомендовано)
npm install --legacy-peer-deps

# Варіант 2: Використати force (якщо legacy-peer-deps не допоміг)
npm install --force
```

**Примітка:** Конфлікт версій між `@nestjs/swagger` та `@nestjs/common` вже виправлено в `package.json`. Якщо все одно виникають проблеми, використайте `--legacy-peer-deps`.

### Крок 2: Налаштування середовища

1. Створіть файл `.env` в папці `backend`:

**Windows (PowerShell):**
```powershell
# Скопіюйте .env.example в .env
Copy-Item .env.example .env
```

**macOS/Linux:**
```bash
# Скопіюйте .env.example в .env
cp .env.example .env
```

2. Відредагуйте `.env` файл та налаштуйте необхідні змінні:

**Обов'язкові змінні (мінімум для запуску):**
```env
# Порт сервера
PORT=3000

# MongoDB (локальна або Atlas)
MONGODB_URI=mongodb://localhost:27017/kraina-mriy

# JWT ключі (згенеруйте надійні ключі!)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=7d
JWT_REFRESH_SECRET=your-super-secret-refresh-key-change-this-in-production
JWT_REFRESH_EXPIRES_IN=30d

# Admin credentials (для першого входу в адмін-панель)
ADMIN_EMAIL=admin@kraina-mriy.com
ADMIN_PASSWORD=admin123

# CORS (URL frontend додатку)
CORS_ORIGIN=http://localhost:5173
```

**Додаткові змінні (опційно):**
```env
# File upload
UPLOAD_DEST=./uploads
MAX_FILE_SIZE=10485760
ALLOWED_IMAGE_TYPES=jpg,jpeg,png,gif,webp
ALLOWED_VIDEO_TYPES=mp4,mov,avi,mkv,webm

# Firebase (для push-повідомлень, опційно)
FIREBASE_PROJECT_ID=your-firebase-project-id
FIREBASE_PRIVATE_KEY=your-firebase-private-key
FIREBASE_CLIENT_EMAIL=your-firebase-client-email
```

**Примітка:** Детальний список всіх змінних дивіться в `backend/.env.example`

### Крок 3: Запуск MongoDB

**Варіант 1: Локальна MongoDB**

```bash
# Windows
mongod

# macOS/Linux
sudo mongod
# або якщо встановлено через Homebrew:
brew services start mongodb-community
```

**Варіант 2: MongoDB Atlas (хмарна БД)**

1. Створіть акаунт на https://www.mongodb.com/cloud/atlas
2. Створіть кластер
3. Отримайте connection string
4. Вставте в `MONGODB_URI` в `.env`

### Крок 4: Запуск Backend сервера

```bash
# Режим розробки (з hot reload)
npm run start:dev

# Або продакшн режим
npm run build
npm run start:prod
```

✅ **Backend запущено!** Сервер доступний на: `http://localhost:3000`

API доступний на: `http://localhost:3000/api/v1`

### Перевірка роботи Backend:

Відкрийте в браузері: `http://localhost:3000/api/v1` (має повернути JSON з інформацією про API)

---

## 2️⃣ Frontend (React + Vite + Material-UI)

### Крок 1: Встановлення залежностей

```bash
cd frontend
npm install
```

### Крок 2: Налаштування середовища

1. Створіть файл `.env` в папці `frontend`:

**Windows (PowerShell):**
```powershell
# Скопіюйте .env.example в .env
Copy-Item .env.example .env
```

**macOS/Linux:**
```bash
# Скопіюйте .env.example в .env
cp .env.example .env
```

2. Відредагуйте `.env` файл:

```env
# URL Backend API
# Для розробки (локальний сервер):
VITE_API_URL=http://localhost:3000/api/v1

# Для продакшну:
# VITE_API_URL=https://api.kraina-mriy.com/api/v1
```

**Примітки:**
- Якщо Backend працює на іншому порту, змініть URL відповідно
- Vite використовує префікс `VITE_` для змінних середовища
- Після зміни `.env` файлу перезапустіть dev сервер

### Крок 3: Запуск Frontend

```bash
# Режим розробки
npm run dev
```

✅ **Frontend запущено!** Додаток доступний на: `http://localhost:5173`

### Перевірка роботи Frontend:

1. Відкрийте браузер: `http://localhost:5173`
2. Має відкритися сторінка входу в адмін-панель
3. Увійдіть з даними з `.env` (ADMIN_EMAIL та ADMIN_PASSWORD)

### Додаткові команди:

```bash
# Збірка для продакшну
npm run build

# Перегляд збірки
npm run preview

# Лінтер
npm run lint

# Форматування коду
npm run format
```

---

## 3️⃣ Mobile (React Native)

### Крок 1: Встановлення залежностей

```bash
cd mobile
npm install
```

### Крок 2: Налаштування API URL

Відредагуйте файл `mobile/src/constants/config.ts`:

**Для Android емулятора:**
```typescript
export const API_BASE_URL = __DEV__
  ? 'http://10.0.2.2:3000/api/v1'  // Android емулятор
  : 'https://api.kraina-mriy.com/api/v1';
```

**Для iOS емулятора:**
```typescript
export const API_BASE_URL = __DEV__
  ? 'http://localhost:3000/api/v1'  // iOS емулятор
  : 'https://api.kraina-mriy.com/api/v1';
```

**Для реального пристрою (Android/iOS):**
```typescript
export const API_BASE_URL = __DEV__
  ? 'http://192.168.1.XXX:3000/api/v1'  // Замініть XXX на IP вашого комп'ютера
  : 'https://api.kraina-mriy.com/api/v1';
```

**Як дізнатися IP адресу вашого комп'ютера:**

**Windows:**
```powershell
ipconfig
# Шукайте "IPv4 Address" в розділі вашого мережевого адаптера
```

**macOS/Linux:**
```bash
ifconfig
# Або
ip addr show
# Шукайте inet адресу (зазвичай 192.168.x.x або 10.0.x.x)
```

**Важливо:**
- Android емулятор використовує `10.0.2.2` для доступу до localhost комп'ютера
- iOS емулятор може використовувати `localhost`
- Реальні пристрої потребують IP адресу комп'ютера в локальній мережі
- Переконайтеся, що пристрій і комп'ютер в одній Wi-Fi мережі
- Перевірте, чи firewall не блокує з'єднання

### Крок 3: Налаштування Firebase (опційно, для push-повідомлень)

1. Створіть Firebase проект на https://console.firebase.google.com/
2. Додайте Android app та iOS app
3. Завантажте конфігураційні файли:
   - `google-services.json` → `mobile/android/app/`
   - `GoogleService-Info.plist` → `mobile/ios/`

### Крок 4: Встановлення iOS залежностей (тільки для macOS)

**Якщо ви розробляєте для iOS:**

1. Переконайтеся, що встановлено CocoaPods:
```bash
sudo gem install cocoapods
```

2. Встановіть iOS залежності:
```bash
cd ios
pod install
cd ..
```

**Примітка:** Після встановлення нових npm пакетів, які мають нативні iOS залежності, потрібно знову запустити `pod install`.

### Крок 5: Запуск Metro Bundler

**Важливо:** Metro Bundler має бути запущений перед запуском додатку на пристрої.

В окремому терміналі:

```bash
cd mobile
npm start
```

Або:

```bash
npx react-native start
```

**Корисні команди для Metro:**
- `r` - перезавантажити додаток
- `d` - відкрити меню розробника
- `i` - відкрити iOS емулятор
- `a` - відкрити Android емулятор
- `c` - очистити кеш та перезапустити

### Крок 6: Запуск на пристрої/емуляторі

**Для Android:**

1. Переконайтеся, що:
   - Android Studio встановлено та запущено
   - Android SDK налаштовано
   - Емулятор створено та запущено, АБО підключено реальний пристрій з увімкненим USB debugging

2. Запустіть додаток:
```bash
npm run android
# Або
npx react-native run-android
```

**Для iOS (тільки macOS):**

1. Переконайтеся, що:
   - Xcode встановлено
   - CocoaPods залежності встановлено (`pod install`)
   - iOS Simulator доступний, АБО підключено реальний пристрій

2. Запустіть додаток:
```bash
npm run ios
# Або
npx react-native run-ios
```

**Для запуску на конкретному емуляторі/пристрої:**

**Android:**
```bash
# Список доступних пристроїв
adb devices

# Запуск на конкретному пристрої
npx react-native run-android --deviceId=DEVICE_ID
```

**iOS:**
```bash
# Список доступних симуляторів
xcrun simctl list devices

# Запуск на конкретному симуляторі
npx react-native run-ios --simulator="iPhone 15 Pro"
```

✅ **Mobile додаток запущено!**

---

## 🔧 Усунення проблем

### Проблеми з авторизацією адміністратора

1. **"Invalid credentials" при вході:**
   - Перевірте, чи адміністратор створено (дивіться логи при запуску)
   - Перевірте `.env` файл: `ADMIN_EMAIL` та `ADMIN_PASSWORD`
   - Спробуйте увійти використовуючи:
     - **Username:** частина email до `@` (наприклад, якщо email `admin@kraina-mriy.com`, то username `admin`)
     - **Email:** повний email з `.env`
     - **Password:** значення з `ADMIN_PASSWORD` в `.env`

2. **Скидання пароля адміністратора:**
   ```bash
   cd backend
   npm run reset-admin
   ```
   Це встановить пароль з `.env` файлу для адміністратора.

3. **Створення нового адміністратора через API:**
   ```bash
   POST http://localhost:3000/api/v1/auth/admin/create-first
   Content-Type: application/json
   
   {
     "username": "admin",
     "password": "admin123"
   }
   ```
   **Примітка:** Це працює тільки якщо в базі немає жодного адміністратора.

4. **Перевірка існуючих адміністраторів:**
   - Подивіться логи сервера при спробі входу - там будуть показані всі адміністратори
   - Або використайте MongoDB Compass/Studio 3T для перегляду колекції `adminusers`

### Backend не запускається

1. **Помилка підключення до MongoDB:**
   - Перевірте, чи запущено MongoDB
   - Перевірте `MONGODB_URI` в `.env`
   - Для MongoDB Atlas: перевірте, чи IP додано до whitelist

2. **Помилка "Port already in use":**
   - Змініть `PORT` в `.env`
   - Або зупиніть процес, який використовує порт 3000

3. **Помилка з залежностями:**
   ```bash
   # Очистка та перевстановлення
   rm -rf node_modules package-lock.json
   npm install
   
   # Якщо все одно помилка конфлікту версій:
   npm install --legacy-peer-deps
   ```

4. **Помилка ERESOLVE (конфлікт версій залежностей):**
   ```bash
   # Використайте legacy-peer-deps
   npm install --legacy-peer-deps
   
   # Або force (менш рекомендовано)
   npm install --force
   ```

### Frontend не підключається до Backend

1. **Перевірте, чи Backend запущено:**
   - Відкрийте `http://localhost:3000/api/v1` в браузері
   - Має повернутися JSON з інформацією про API

2. **Перевірте `VITE_API_URL` в `.env`:**
   ```env
   VITE_API_URL=http://localhost:3000/api/v1
   ```
   - Переконайтеся, що URL правильний
   - Після зміни `.env` перезапустіть dev сервер

3. **Перевірте CORS налаштування в Backend:**
   - В `.env` файлі Backend має бути: `CORS_ORIGIN=http://localhost:5173`
   - Перезапустіть Backend після зміни

4. **Перевірте помилки в браузері:**
   - Відкрийте DevTools (F12) → Console → перевірте помилки
   - Відкрийте DevTools → Network → перевірте failed запити
   - Перевірте, чи запити йдуть на правильний URL

5. **Очистка кешу:**
   ```bash
   # Видаліть node_modules та перевстановіть
   rm -rf node_modules package-lock.json
   npm install
   ```

### Mobile не підключається до Backend

1. **Перевірте налаштування API URL:**
   - Відкрийте `mobile/src/constants/config.ts`
   - Переконайтеся, що `API_BASE_URL` правильний для вашого пристрою

2. **Android емулятор:**
   - Використовуйте `http://10.0.2.2:3000/api/v1` (не `localhost`!)
   - Перевірте, чи Backend запущено
   - Перевірте, чи AndroidManifest.xml має `usesCleartextTraffic="true"`

3. **iOS емулятор:**
   - Використовуйте `http://localhost:3000/api/v1`
   - Перевірте, чи Backend запущено
   - Перевірте Info.plist для дозволу HTTP (якщо потрібно)

4. **Реальний пристрій:**
   - Використовуйте IP адресу вашого комп'ютера (наприклад, `http://192.168.1.100:3000/api/v1`)
   - Переконайтеся, що пристрій і комп'ютер в одній Wi-Fi мережі
   - Перевірте firewall налаштування Windows/macOS
   - Перевірте, чи Backend доступний з пристрою (відкрийте URL в браузері пристрою)

5. **Помилка "Network request failed":**
   - Перевірте URL в `config.ts`
   - Перевірте, чи Backend запущено та доступний
   - Для Android: додайте в `android/app/src/main/AndroidManifest.xml`:
     ```xml
     <application
       android:usesCleartextTraffic="true"
       ...>
     ```
   - Перевірте логи Metro Bundler та React Native Debugger

6. **Тестування з'єднання:**
   ```bash
   # На пристрої відкрийте браузер та перейдіть на:
   # http://[YOUR_IP]:3000/api/v1
   # Має повернутися JSON
   ```

### Metro Bundler не запускається

```bash
# Очистка кешу
npm start -- --reset-cache

# Або
npx react-native start --reset-cache
```

### Помилки з iOS CocoaPods

```bash
cd ios
pod deintegrate
pod install
cd ..
```

---

## 📝 Порядок запуску (швидка довідка)

### Повний запуск (всі сервіси):

**Термінал 1: Backend**
```bash
cd backend
npm install
# Windows:
Copy-Item .env.example .env
# macOS/Linux:
# cp .env.example .env
# Відредагуйте .env файл
npm run start:dev
```

**Термінал 2: Frontend**
```bash
cd frontend
npm install
# Windows:
Copy-Item .env.example .env
# macOS/Linux:
# cp .env.example .env
# Відредагуйте .env файл (VITE_API_URL)
npm run dev
```

**Термінал 3: Mobile Metro Bundler**
```bash
cd mobile
npm install
# Налаштуйте mobile/src/constants/config.ts (API_BASE_URL)
npm start
```

**Термінал 4: Mobile (Android або iOS)**
```bash
cd mobile
npm run android  # або npm run ios
```

### Мінімальний запуск (тільки Backend + Frontend):

```bash
# Термінал 1: Backend
cd backend && npm install && npm run start:dev

# Термінал 2: Frontend  
cd frontend && npm install && npm run dev
```

---

## 🌐 Доступні URL

Після запуску всіх сервісів:

- **Backend API:** http://localhost:3000/api/v1
- **Frontend (Admin Panel):** http://localhost:5173
- **Mobile:** Запускається на пристрої/емуляторі

---

## 📚 Додаткова інформація

### Структура проекту:

```
.
├── backend/          # NestJS Backend API
├── frontend/         # React Admin Panel
└── mobile/           # React Native Mobile App
```

### Корисні команди:

**Backend:**
- `npm run start:dev` - режим розробки
- `npm run build` - збірка
- `npm run start:prod` - продакшн режим
- `npm run lint` - лінтер
- `npm test` - тести

**Frontend:**
- `npm run dev` - режим розробки
- `npm run build` - збірка
- `npm run preview` - перегляд збірки
- `npm run lint` - лінтер

**Mobile:**
- `npm start` - Metro Bundler
- `npm run android` - запуск на Android
- `npm run ios` - запуск на iOS
- `npm test` - тести

---

## 🆘 Підтримка

Якщо виникли проблеми:

1. Перевірте версії Node.js та npm
2. Перевірте налаштування в `.env` файлах
3. Перевірте логи в терміналі
4. Перевірте документацію в `README.md` файлах кожної частини проекту

---

**Успішного запуску! 🚀**

