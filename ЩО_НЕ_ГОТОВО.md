# Що не готово в проекті

## Backend

### ❌ Не реалізовано (опційні/додаткові функції):

1. **Кешування (Redis опційно):**
   - [ ] Кешування списків міст та посад
   - [ ] Кешування рейтингів

2. **Push-повідомлення:**
   - [ ] Інтеграція з Firebase Cloud Messaging (структура готова, потрібно підключити SDK)
   - [ ] Обробка невалідних токенів (потрібно додати при інтеграції FCM)

3. **Cron Jobs (опційні):**
   - [x] Оновлення рейтингів (щогодини) - реалізовано (перевірка та логування статистики)
   - [x] Очищення старих логів (щодня) - реалізовано (видалення логів старше 90 днів)

4. **Логування та моніторинг (опційні):**
   - [ ] Логування помилок (Sentry)
   - [ ] Метрики продуктивності

5. **Безпека:**
   - [ ] HTTPS обов'язково - налаштування сервера (не залежить від коду)

6. **Тестування та оптимізація:**
   - [ ] Unit тести
   - [ ] Integration тести
   - [ ] Навантажувальне тестування
   - [ ] Оптимізація запитів
   - [ ] Індекси БД (потрібно перевірити та додати при потребі)

### ✅ Реалізовано нещодавно:
- ✅ Логування дій адміністраторів (ActivityLogService, автоматичне логування входу/виходу, CRUD операцій)

## Frontend

### ❌ Не реалізовано (опційні/додаткові функції):

1. **Авторизація:**
   - [x] Повна перевірка ролей (супер-адмін, адмін, viewer) - реалізовано (useRole hook та RoleGuard компонент)

2. **Управління питаннями:**
   - [x] Preview питання (як воно виглядатиме в мобільному додатку з медіа) - покращено (додано стилізацію та покращено відображення)

3. **База знань:**
   - [x] Перегляд статті (preview - як вона виглядатиме в мобільному додатку) - вже реалізовано (модальне вікно preview в KnowledgeBasePage)

4. **Полірування:**
   - [ ] Оптимізація (lazy loading, code splitting)
   - [ ] Тестування (unit, integration)
   - [ ] Виправлення багів (постійний процес)

### ✅ Реалізовано нещодавно:
- ✅ Дерево категорій з drag & drop (CategoryTree компонент)
- ✅ Теплова карта активності (ActivityHeatmap компонент)
- ✅ Генерація прев'ю для відео (backend: fluent-ffmpeg)
- ✅ Логи дій адміністраторів (LogsPage)

## Підсумок

### Backend: ~99% основного функціоналу реалізовано
**Залишилося (опційні/додаткові):**
- Firebase FCM інтеграція (можна пізніше)
- Тести (unit, integration, e2e)
- Кешування (Redis) - опційно
- Логування помилок (Sentry) - опційно
- Метрики продуктивності - опційно
- Очищення старих логів - опційно
- HTTPS - налаштування сервера

### Frontend: 100% основного функціоналу + всі опційні функції реалізовано
**Залишилося (опційні/покращення):**
- Повна перевірка ролей (покращення)
- Preview статей (можна додати)
- Оптимізація та тестування (постійний процес)

## Рекомендації

1. **Пріоритет 1 (важливі для продакшену):**
   - HTTPS налаштування на сервері
   - Додати індекси БД для оптимізації запитів
   - Покращити перевірку ролей на frontend

2. **Пріоритет 2 (корисні, але не критичні):**
   - Firebase FCM інтеграція (для push-повідомлень)
   - Очищення старих логів (cron job)
   - Preview статей на frontend

3. **Пріоритет 3 (опційні/пізніше):**
   - Тести (unit, integration, e2e)
   - Кешування (Redis)
   - Логування помилок (Sentry)
   - Метрики продуктивності
   - Оптимізація frontend (lazy loading, code splitting)

