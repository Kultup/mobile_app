# План розробки Backend (API сервер)

## Технологічний стек

- **Мова:** Node.js / TypeScript
- **Фреймворк:** NestJS (рекомендовано) або Express.js
- **База даних:** MongoDB
- **ODM:** Mongoose
- **Аутентифікація:** JWT (jsonwebtoken)
- **Валідація:** class-validator, class-transformer
- **Логування:** Winston або Pino
- **Тестування:** Jest
- **Cron Jobs:** node-cron

## Основні компоненти

### 1. Структура проекту

```
backend/
├── src/
│   ├── auth/              # Аутентифікація та авторизація
│   ├── users/             # Користувачі
│   ├── questions/         # Питання та категорії
│   ├── tests/             # Тестування
│   ├── knowledge-base/    # База знань
│   ├── ratings/           # Рейтинги
│   ├── surveys/           # Опитування
│   ├── achievements/      # Ачівки та гейміфікація
│   ├── shop/              # Внутрішній магазин
│   ├── admin/             # Адмін API
│   ├── push/              # Push-повідомлення
│   ├── files/             # Завантаження файлів
│   ├── cron/              # Cron jobs
│   ├── common/            # Спільні модулі
│   └── main.ts            # Точка входу
├── test/                  # Тести
├── migrations/            # Міграції БД
└── package.json
```

## Функціонал для реалізації

### 2. Аутентифікація та авторизація

**Endpoints:**
- `POST /api/v1/auth/register` - Реєстрація користувача
- `POST /api/v1/auth/login` - Вхід користувача
- `POST /api/v1/auth/refresh` - Оновлення токену
- `POST /api/v1/auth/logout` - Вихід
- `POST /api/v1/admin/auth/login` - Вхід адміністратора

**Завдання:**
- [x] Реалізувати реєстрацію з перевіркою дублювання (ПІБ + місто + посада)
- [x] JWT токени (access + refresh)
- [x] Middleware для перевірки токенів (JwtAuthGuard, RolesGuard)
- [x] Ролі та права доступу (користувач, адмін, супер-адмін)
- [x] Хешування паролів (bcrypt)

### 3. Користувачі

**Endpoints:**
- `GET /api/v1/user/profile` - Профіль користувача
- `PUT /api/v1/user/profile` - Оновлення профілю
- `GET /api/v1/admin/users` - Список користувачів (адмін)
- `PUT /api/v1/admin/users/:id` - Редагування користувача (адмін)
- `DELETE /api/v1/admin/users/:id` - Видалення/блокування (адмін)

**Завдання:**
- [x] CRUD операції для користувачів
- [x] Фільтрація за містом, посадою, активністю
- [x] Пошук користувачів
- [x] Оновлення статистики (балів, streak) - автоматично після завершення тесту

### 4. Довідники

**Endpoints:**
- `GET /api/v1/cities` - Список міст
- `GET /api/v1/positions` - Список посад

**Завдання:**
- [x] CRUD для міст та посад (адмін) - CitiesController, PositionsController
- [ ] Кешування списків (Redis опційно)

### 5. Питання та категорії

**Endpoints:**
- `GET /api/v1/admin/question-categories` - Категорії питань
- `POST /api/v1/admin/question-categories` - Створити категорію
- `GET /api/v1/admin/questions` - Список питань
- `POST /api/v1/admin/questions` - Створити питання
- `PUT /api/v1/admin/questions/:id` - Оновити питання
- `DELETE /api/v1/admin/questions/:id` - Видалити питання
- `POST /api/v1/admin/questions/import` - Імпорт з Excel

**Завдання:**
- [x] CRUD для категорій (дерево з parent_id) - QuestionCategory schema
- [x] CRUD для питань з варіантами відповідей (embedded) - QuestionsService
- [x] Валідація питань (обов'язково має бути правильна відповідь) - в DTO та сервісі
- [ ] Масовий імпорт з Excel
- [x] Фільтрація та пошук питань - реалізовано в QuestionsService

### 6. Тестування

**Endpoints:**
- `GET /api/v1/tests/daily` - Отримати щоденний тест
- `POST /api/v1/tests/:id/start` - Почати тест
- `POST /api/v1/tests/:id/answer` - Відправити відповідь
- `POST /api/v1/tests/:id/complete` - Завершити тест
- `GET /api/v1/tests/history` - Історія тестів

**Завдання:**
- [x] Генерація щоденних тестів (5 питань, по 1-2 з різних категорій) - CronService
- [x] Логіка вибірки питань (уникнення повторів) - TestsService
- [x] Перевірка відповідей та нарахування балів - TestsService.submitAnswer
- [x] Оновлення рейтингу після завершення тесту - TestsService.completeTest
- [x] Перевірка дедлайну (23:59) - в логіці тестів
- [x] Оновлення streak (серія днів поспіль) - TestsService.updateUserStatistics

### 7. База знань

**Endpoints:**
- `GET /api/v1/knowledge-base/categories` - Категорії (дерево)
- `GET /api/v1/knowledge-base/articles` - Список статей
- `GET /api/v1/knowledge-base/articles/:id` - Деталі статті
- `POST /api/v1/admin/knowledge-base/articles` - Створити статтю (адмін)
- `PUT /api/v1/admin/knowledge-base/articles/:id` - Оновити (адмін)
- `DELETE /api/v1/admin/knowledge-base/articles/:id` - Видалити (адмін)

**Завдання:**
- [x] CRUD для категорій бази знань - KnowledgeBaseCategory schema
- [x] CRUD для статей (Markdown/HTML контент) - KnowledgeBaseService
- [x] Завантаження зображень та PDF - FilesService (підтримка зображень)
- [ ] Text search (MongoDB text index) - потрібно додати індекси
- [x] Підрахунок переглядів - реалізовано в KnowledgeBaseService

### 8. Рейтинги та статистика

**Endpoints:**
- `GET /api/v1/ratings/global` - Глобальний рейтинг
- `GET /api/v1/ratings/by-city` - Рейтинг за містом
- `GET /api/v1/ratings/by-position` - Рейтинг за посадою
- `GET /api/v1/user/statistics` - Особиста статистика
- `GET /api/v1/admin/statistics/dashboard` - Дашборд (адмін)
- `GET /api/v1/admin/statistics/export` - Експорт статистики (адмін)

**Завдання:**
- [x] Aggregation pipeline для рейтингів - RatingsService
- [x] Підрахунок позиції користувача - RatingsService
- [x] Статистика (проценти, streak, графіки) - UsersService, AdminService
- [ ] Експорт у Excel/CSV
- [ ] Кешування рейтингів (Redis опційно)

### 9. Опитування та зворотний зв'язок

**Endpoints:**
- `GET /api/v1/surveys` - Активні опитування
- `POST /api/v1/surveys/:id/respond` - Відправити відповідь
- `POST /api/v1/feedback/question` - Запропонувати питання
- `POST /api/v1/feedback/report-error` - Повідомити про помилку
- `POST /api/v1/admin/surveys` - Створити опитування (адмін)
- `GET /api/v1/admin/surveys/:id/responses` - Відповіді (адмін)

**Завдання:**
- [x] CRUD для опитувань - SurveysService (базова реалізація)
- [x] Збереження відповідей користувачів - SurveysService.respond
- [x] Перевірка унікальності (одна відповідь на опитування) - в SurveysService
- [ ] Експорт відповідей

### 9.1. Гейміфікація та ачівки

**Endpoints:**
- `GET /api/v1/achievements` - Список ачівок користувача
- `GET /api/v1/achievements/all` - Всі доступні ачівки
- `GET /api/v1/achievements/:id` - Деталі ачівки
- `POST /api/v1/admin/achievements` - Створити ачівку (адмін)
- `PUT /api/v1/admin/achievements/:id` - Оновити ачівку (адмін)
- `GET /api/v1/user/achievements/progress` - Прогрес до ачівок

**Завдання:**
- [x] Модель ачівок (назва, опис, умови, нагорода) - Achievement, UserAchievement schemas
- [x] Відстеження прогресу користувачів до ачівок - UserAchievement schema
- [x] Автоматична перевірка умов отримання ачівок - AchievementsCheckerService
- [x] Нарахування балів за отримання ачівок - AchievementsCheckerService
- [x] Push-повідомлення при отриманні ачівки - AchievementsCheckerService
- [x] CRUD для ачівок (адмін) - AchievementsService

**Типи ачівок:**
- За кількість тестів (10, 50, 100, 500)
- За streak (7, 30, 100 днів)
- За точність (5/5, 10 тестів поспіль на 5/5)
- За рейтинг (топ-100, топ-10, перше місце)
- Спеціальні (ювілейні, події)

### 9.2. Внутрішній магазин

**Endpoints:**
- `GET /api/v1/shop/products` - Список товарів
- `GET /api/v1/shop/products/:id` - Деталі товару
- `POST /api/v1/shop/purchase` - Купити товар
- `GET /api/v1/shop/purchases` - Історія покупок
- `GET /api/v1/user/balance` - Баланс балів користувача
- `POST /api/v1/admin/shop/products` - Створити товар (адмін)
- `PUT /api/v1/admin/shop/products/:id` - Оновити товар (адмін)
- `DELETE /api/v1/admin/shop/products/:id` - Видалити товар (адмін)

**Завдання:**
- [x] Модель товарів (назва, опис, ціна, тип, зображення) - ShopProduct schema
- [x] Категорії товарів - поле category в ShopProduct
- [x] Система покупок (перевірка балансу, списання балів) - ShopService.purchase
- [x] Збереження куплених товарів користувача - UserPurchase schema
- [x] Застосування товарів (аватарки, рамки, теми) - ShopService.applyProduct
- [x] Історія покупок - ShopService.getPurchases
- [x] CRUD для товарів (адмін) - ShopService (потрібно додати адмін endpoints)

**Типи товарів:**
- Аватарки (avatar)
- Рамки профілю (profile_frame)
- Бейджі (badge)
- Темна тема (theme)
- Кастомізація (customization)
- Подарунки (gift)

### 10. Push-повідомлення

**Endpoints:**
- `POST /api/v1/push-tokens` - Зареєструвати токен
- `DELETE /api/v1/push-tokens/:token` - Видалити токен

**Завдання:**
- [x] Збереження push-токенів (iOS, Android) - PushToken schema, PushService
- [ ] Інтеграція з Firebase Cloud Messaging (структура готова, потрібно підключити SDK)
- [x] Розсилка push-повідомлень - PushService.sendNotification (базова структура)
- [ ] Обробка невалідних токенів (потрібно додати при інтеграції FCM)

### 11. Завантаження файлів

**Endpoints:**
- `POST /api/v1/admin/files/upload` - Завантажити файл (адмін)
- `POST /api/v1/admin/files/upload-video` - Завантажити відео (адмін)
- `GET /api/v1/files/:id` - Отримати файл
- `GET /api/v1/files/video/:id` - Отримати відео (streaming)

**Завдання:**
- [x] Завантаження зображень (JPG, PNG) - макс. 10MB - FilesService.uploadFile
- [x] Завантаження відео (MP4, MOV) - макс. 50MB - FilesService.uploadVideo
- [ ] Генерація прев'ю для відео (thumbnail) - TODO в коді
- [x] Валідація типів та розмірів файлів - FilesService
- [x] Зберігання на файловому сервері (S3/локально) - локально реалізовано
- [x] Генерація URL для доступу - FilesService повертає URL
- [ ] Оптимізація відео (опційно - конвертація в різні якості)
- [ ] Streaming для великих відео файлів - базовий endpoint є, потрібно доопрацювати

### 12. Налаштування системи

**Endpoints:**
- `GET /api/v1/admin/settings` - Отримати налаштування (адмін)
- `PUT /api/v1/admin/settings/:key` - Оновити налаштування (адмін)

**Завдання:**
- [ ] Зберігання налаштувань у БД
- [ ] Налаштування часу розсилки тестів
- [ ] Налаштування дедлайну
- [ ] Текст push-повідомлень

### 13. Cron Jobs

**Завдання:**
- [x] Генерація щоденних тестів (00:00) - CronService.generateDailyTests
- [x] Розсилка push-повідомлень (12:00) - CronService.sendDailyTestNotifications
- [x] Нагадування за 3 години до дедлайну (21:00) - CronService.sendReminder3Hours
- [x] Нагадування за 1 годину до дедлайну (23:00) - CronService.sendReminder1Hour
- [ ] Оновлення рейтингів (щогодини) - можна додати якщо потрібно
- [ ] Очищення старих логів (щодня)

### 14. Логування та моніторинг

**Завдання:**
- [ ] Логування всіх запитів
- [ ] Логування помилок (Sentry)
- [ ] Логування дій адміністраторів
- [ ] Метрики продуктивності

### 15. Безпека

**Завдання:**
- [ ] HTTPS обов'язково
- [ ] Rate limiting (100 req/min для авторизованих)
- [ ] CORS налаштування
- [ ] Валідація всіх вхідних даних
- [ ] Захист від NoSQL injection
- [ ] Sanitization даних

## Етапи розробки

### Етап 1: Базова інфраструктура (1-2 тижні) ✅
- [x] Налаштування проекту (NestJS/Express)
- [x] Підключення MongoDB
- [x] Створення моделей (Mongoose schemas)
- [x] Базова аутентифікація (JWT)
- [x] Структура проекту

### Етап 2: Користувачі та авторизація (1 тиждень) ✅
- [x] Реєстрація та вхід
- [x] Профіль користувача
- [x] Адмін авторизація
- [x] Довідники (міста, посади)

### Етап 3: Питання та тести (2 тижні) ✅
- [x] CRUD для питань та категорій
- [x] Генерація щоденних тестів
- [x] Проходження тестів
- [x] Підрахунок балів

### Етап 4: Рейтинги та статистика (1 тиждень) ✅
- [x] Глобальний рейтинг
- [x] Рейтинги за містом/посадою
- [x] Особиста статистика
- [x] Адмін дашборд

### Етап 5: База знань (1 тиждень) ✅
- [x] CRUD для статей
- [x] Завантаження файлів
- [ ] Пошук (потрібно додати text index)

### Етап 6: Push-повідомлення (1 тиждень) ⏳
- [x] Реєстрація токенів
- [ ] Інтеграція з FCM (структура готова, потрібно підключити SDK)
- [x] Розсилка повідомлень (базова структура)

### Етап 7: Cron Jobs (3-5 днів) ✅
- [x] Генерація тестів
- [x] Розсилка push
- [x] Нагадування

### Етап 8: Опитування та зворотний зв'язок (1 тиждень) ✅
- [x] CRUD опитувань
- [x] Відповіді користувачів
- [x] Форми зворотного зв'язку

### Етап 8.1: Гейміфікація та ачівки (1 тиждень) ✅
- [x] Модель ачівок
- [x] Система відстеження прогресу
- [x] Автоматична перевірка умов
- [x] API для ачівок
- [x] Нарахування балів за ачівки

### Етап 8.2: Внутрішній магазин (1 тиждень) ✅
- [x] Модель товарів
- [x] Система покупок
- [x] Управління балансом балів
- [x] API для магазину
- [x] Застосування товарів

### Етап 9: Тестування та оптимізація (1-2 тижні)
- [ ] Unit тести
- [ ] Integration тести
- [ ] Навантажувальне тестування
- [ ] Оптимізація запитів
- [ ] Індекси БД

## Залежності

**Перед початком розробки:**
- MongoDB встановлена та налаштована
- Firebase проект створений (для FCM)
- Файловий сервер налаштований (S3 або локально)

**Паралельно можна розробляти:**
- Frontend (після готовності базових endpoints)
- Mobile (після готовності базових endpoints)

## API документація

- Використовувати Swagger/OpenAPI для автоматичної генерації документації
- Документувати всі endpoints з прикладами

## Статус реалізації

### ✅ Повністю реалізовано:
- Базова інфраструктура (NestJS, MongoDB, структура проекту)
- Аутентифікація та авторизація (JWT, ролі, guards)
- Користувачі (CRUD, фільтрація, пошук, статистика)
- Довідники (міста, посади - CRUD)
- Питання та категорії (CRUD, валідація, фільтрація)
- Тестування (генерація щоденних тестів, проходження, підрахунок балів, streak)
- База знань (CRUD статей, перегляди)
- Рейтинги та статистика (глобальний, за містом/посадою, дашборд)
- Опитування та зворотний зв'язок (CRUD, відповіді, feedback)
- Гейміфікація та ачівки (модель, відстеження, автоматична перевірка)
- Внутрішній магазин (товари, покупки, баланс, застосування)
- Завантаження файлів (фото/відео з валідацією)
- Cron Jobs (генерація тестів, нагадування)
- Адмін панель API (CRUD користувачів, адмінів, дашборд)

### ⏳ Частково реалізовано:
- Push-повідомлення (реєстрація токенів, структура відправки - потрібна інтеграція FCM SDK)
- Налаштування системи (схема є, потрібні endpoints)

### ❌ Не реалізовано:
- Firebase FCM інтеграція (потрібно підключити SDK)
- Тести (unit, integration, e2e)
- Генерація thumbnail для відео
- Оптимізація відео (конвертація в різні якості)
- Streaming для великих відео файлів
- Кешування (Redis)
- Логування та моніторинг (Sentry, метрики)
- Очищення старих логів

### ✅ Додатково реалізовано:
- Swagger документація (доступна на /api/docs)
- Масовий імпорт питань з Excel (POST /api/v1/questions/import)
- Експорт статистики у Excel (GET /api/v1/admin/statistics/export)
- Експорт історії тестів у Excel (GET /api/v1/admin/tests/export)
- Експорт відповідей опитувань у Excel (GET /api/v1/admin/surveys/:id/responses/export)
- Text search для бази знань (MongoDB text index додано)
- Rate limiting (100 запитів/хвилину через ThrottlerGuard)
- Налаштування системи (endpoints для управління налаштуваннями)

### 📊 Прогрес: ~95% основного функціоналу реалізовано

